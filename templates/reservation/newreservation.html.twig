{% extends 'base.html.twig' %}
{% block body %}
    <h1> Ajouter une Reservation </h1>
    {{form_start(form)}}
    {{form_widget(form)}}
    {{form_end(form)}}

    <a class="btn btn-danger" href="{{ path('listres') }}"> Go TO List </a>

{% endblock %}